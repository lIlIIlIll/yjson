# yjson 2.0 reviewed breaking API inventory. The complete declaration list is
# release/public-api-snapshot.txt.
inventory_version = 1
release_version = "2.0.0"
package_manifest_version = "2.0.0"
status = "2.0-release-ready"
compatibility_policy = "2.0 is a breaking redesign; no deprecated aliases or compatibility shims are provided"

[package_pairing]
core = "2.0.0"
macros = "2.0.0"
aggregate = "2.0.0"
native = "2.0.0"
native_accel = "2.0.0"
backends = "2.0.0"
algorithms = "2.0.0"
schema_formats = "2.0.0"
yyjson = "2.0.0"
rule = "yjson and yjson_macros are one release unit; generated support protocol v1 may remain compatible across patch releases"

[[api]]
package = "yjson"
kind = "cangjie-limits"
symbol = "JsonReadLimits / JsonWriteLimits"
signature = "JsonReadConfig(limits!: JsonReadLimits); JsonWriteConfig(..., limits!: JsonWriteLimits)"
source = ["src/lib_json_value.cj", "src/lib_json_stream_writer.cj"]
needle = "public class Json"
stability = "2.0-stable"
compatibility = "breaking composition of resource limits"
contract = "Pure, generated, stream, native, and advanced backends interpret the same limits objects."
consumer = "All readers and writers"

[[api]]
package = "yjson"
kind = "cangjie-interface"
symbol = "JsonDocument"
signature = "public interface JsonDocument"
source = "src/lib_json_document.cj"
needle = "public interface JsonDocument"
stability = "2.0-stable"
compatibility = "breaking removal of Resource, close, isClosed, and backend identity"
contract = "The default document is a GC-managed read-only Compact representation."
consumer = "YJson.parseDocument callers"

[[api]]
package = "yjson"
kind = "cangjie-overload-family"
symbol = "YJson.parseDocument / toStream / fromStream"
signature = "Default API has no backend parameter"
source = "src/lib_json_direct_codec.cj"
needle = "public static func parseDocument(text: String,"
stability = "2.0-stable"
compatibility = "breaking removal of backend selection from ordinary APIs"
contract = "parseDocument returns managed Compact; stream calls remain incremental."
consumer = "Ordinary applications"

[[api]]
package = "yjson"
kind = "generated-protocol"
symbol = "GeneratedSupportV1"
signature = "public class GeneratedSupportV1"
source = "src/lib_json_generated_support_v1.cj"
needle = "public class GeneratedSupportV1"
stability = "protocol-v1"
compatibility = "breaking replacement of concrete generated reader/writer references"
contract = "Generated source embeds protocol 1 and fails clearly when the runtime protocol differs."
consumer = "yjson_macros generated codecs"

[[api]]
package = "yjson"
kind = "startup-runtime"
symbol = "JsonAccelerationException / initializeYJsonAccelerationV1"
signature = "Process state Unconfigured -> PureFrozen | NativeFrozen"
source = "src/lib_json_runtime.cj"
needle = "public func initializeYJsonAccelerationV1"
stability = "protocol-v1"
compatibility = "new 2.0 startup freeze contract"
contract = "Late initialization, provider conflicts, and ABI/protocol mismatches fail without fallback."
consumer = "yjson_native_accel"

[[api]]
package = "yjson_native_accel"
kind = "cangjie-static-method"
symbol = "YJsonNativeAccel.initialize"
signature = "public static func initialize(): Unit"
source = "packages/yjson_native_accel/src/native_accel.cj"
needle = "public static func initialize(): Unit"
stability = "2.0-stable"
compatibility = "new single-option native startup API"
contract = "Applications call once before any YJson operation and continue using YJson."
consumer = "Applications opting into Custom Native primitives"

[[api]]
package = "yjson"
kind = "native-writer-protocol"
symbol = "JsonNativeWriterBackend / JsonNativeAccelerationPrimitivesV1"
signature = "escapeString(...); formatInt64Array(...); parseInt64Array(...)"
source = "src/lib_json_native_scanner.cj"
needle = "public interface JsonNativeWriterBackend"
stability = "protocol-v1"
compatibility = "new 2.0 internal cross-package acceleration seam"
contract = "Native code provides bounded string and Int64 read/write primitives; core retains cursor/state, separators, limits, normalized errors, and failure policy."
consumer = "yjson_native implementation only"

[[api]]
package = "yjson_backends"
kind = "advanced-backend-family"
symbol = "YJsonAdvanced / BackendJsonDocument"
signature = "YJsonAdvanced.*WithBackend"
source = "packages/yjson_backends/src/backends.cj"
needle = "public class YJsonAdvanced"
stability = "advanced-2.0"
compatibility = "breaking relocation from the default product surface"
contract = "Resource-owning DOM and whole-document stream behavior require explicit advanced APIs."
consumer = "Advanced native and yyjson users"

[[api]]
package = "yjson_algorithms"
kind = "work-limits"
symbol = "JsonPathLimits / JsonPatchLimits / JsonSchemaLimits"
signature = "Finite defaults plus explicit unlimited constants"
source = "packages/yjson_algorithms/src/work_limits.cj"
needle = "public class JsonPathLimits"
stability = "2.0-stable"
compatibility = "breaking relocation and finite safe defaults"
contract = "Budget exhaustion throws JsonWorkLimitException with code work_limit_exceeded."
consumer = "JSONPath, Patch, Merge Patch, and Schema users"

[[api]]
package = "yjson_algorithms"
kind = "resolver-interface"
symbol = "UriResolver"
signature = "public interface UriResolver"
source = "packages/yjson_algorithms/src/lib_json_schema.cj"
needle = "public interface UriResolver"
stability = "2.0-stable"
compatibility = "breaking rename and package relocation"
contract = "External schema URIs resolve only through an injected resolver; no default network access."
consumer = "JSON Schema applications"
