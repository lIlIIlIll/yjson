# Public declarations stabilized or changed for the 1.0.0-rc.1 candidate.
# This is an additive inventory, not a replacement for the complete generated
# symbol inventory used by the SDK ABI tooling.
inventory_version = 1
release_version = "1.0.0-rc.1"
package_manifest_version = "1.0.0"
status = "approved"
compatibility_policy = "1.0.0-rc.1 may differ from pre-1.0 snapshots only when explicitly recorded; inventory changes require review"

[package_pairing]
core = "1.0.0"
macros = "1.0.0"
aggregate = "1.0.0"
native = "1.0.0"
yyjson = "1.0.0"
rule = "Generated macro output and the optional native facade must be consumed with the same yjson core release."

[[api]]
package = "yjson"
kind = "cangjie-instance-method"
symbol = "JsonFastReader.suggestRawCollectionCapacity"
signature = "public func suggestRawCollectionCapacity(): Int64"
source = "src/lib_json_fast_reader.cj"
needle = "public func suggestRawCollectionCapacity(): Int64"
stability = "stable-generated-code-bridge"
compatibility = "additive non-open non-overriding instance method"
contract = "Returns a bounded capacity hint for the unread portion of the current raw array; it does not advance the reader or retain input."
consumer = "Generated yjson_macros fast collection codecs"

[[api]]
package = "yjson_macros"
kind = "generated-source-dependency"
symbol = "reader.suggestRawCollectionCapacity"
signature = "Generated readFast methods call JsonFastReader.suggestRawCollectionCapacity"
source = "packages/yjson_macros/src/json_codec.cj"
needle = "reader.suggestRawCollectionCapacity()"
stability = "stable-generated-code-contract"
compatibility = "requires the matching yjson core release when generated code is compiled"
contract = "The macro emits the bridge only for fast collection readers and keeps the empty-array path allocation-free."
consumer = "Applications using yjson_all or a matching yjson/yjson_macros pair"

[[api]]
package = "yjson_macros"
kind = "cangjie-expression-macro"
symbol = "Json"
signature = "public macro Json(input: Tokens): Tokens"
source = "packages/yjson_macros/src/json_literal.cj"
needle = "public macro Json(input: Tokens): Tokens"
stability = "stable-json-literal-entry"
compatibility = "additive macro; generated code requires the matching yjson core release"
contract = "Accepts JSON-like object, array, scalar, trailing-comma, value-interpolation, and dynamic-key syntax and writes a compact String without building a full JsonNode tree."
consumer = "Applications importing yjson_all or yjson_macros with yjson"

[[api]]
package = "yjson_macros"
kind = "cangjie-expression-macro"
symbol = "JsonValue"
signature = "public macro JsonValue(input: Tokens): Tokens"
source = "packages/yjson_macros/src/json_literal.cj"
needle = "public macro JsonValue(input: Tokens): Tokens"
stability = "stable-json-literal-entry"
compatibility = "additive macro that requires the former JsonValue type to be renamed"
contract = "Builds a mutable JsonNode tree; interpolations run exactly once left-to-right and dynamic key collisions use LastWins."
consumer = "Applications importing yjson_all or yjson_macros with yjson"

[[api]]
package = "yjson"
kind = "cangjie-class-rename"
symbol = "JsonNode"
signature = "public abstract class JsonNode <: ToString"
source = "src/lib_json_value.cj"
needle = "public abstract class JsonNode <: ToString"
stability = "stable-mutable-ast-root"
compatibility = "intentional breaking rename from JsonValue; source and ABI rebuild required"
contract = "Root of the mutable JSON AST and owner of short scalar/container properties plus object/array indexing."
consumer = "Applications using the mutable AST or @JsonValue"

[[api]]
package = "yjson"
kind = "cangjie-overload-family"
symbol = "YJson literal construction bridges"
signature = "YJson.nullValue, array, object, value, writeValue"
source = "src/lib_json_direct_codec.cj"
needle = "public static func nullValue(): JsonNullValue"
stability = "stable-json-literal-runtime-bridge"
compatibility = "additive overloads"
contract = "Provides explicit mutable-node factories, codec-backed conversion, and direct writer dispatch for macro-generated code."
consumer = "@Json and @JsonValue generated code plus fluent application construction"

[[api]]
package = "yjson"
kind = "cangjie-instance-method-family"
symbol = "JsonArrayValue.add/set and JsonObjectValue.put"
signature = "chainable mutable container methods"
source = "src/lib_json_value.cj"
needle = "public func set(index: Int64, value: JsonNode): JsonArrayValue"
stability = "stable-fluent-mutable-ast"
compatibility = "additive add/set overloads; JsonObjectValue.put return type changes from Unit to JsonObjectValue"
contract = "Mutates the receiver and returns it for chaining; generic overloads convert JsonCodecProvider values through YJson.value."
consumer = "Applications constructing mutable JsonNode trees"

[[api]]
package = "yjson"
kind = "cangjie-interface"
symbol = "JsonNativeFloatParserBackend"
signature = "public interface JsonNativeFloatParserBackend"
source = "src/lib_json_native_scanner.cj"
needle = "public interface JsonNativeFloatParserBackend"
stability = "stable-optional-backend-seam"
compatibility = "new interface; existing interfaces and implementations are unchanged"
contract = "A process-level optional parser for an already validated JSON number token."
consumer = "yjson_native and explicitly managed native backends"

[[api]]
package = "yjson"
kind = "cangjie-interface-method"
symbol = "JsonNativeFloatParserBackend.parseFloat64"
signature = "func parseFloat64(input: CPointer<UInt8>, len: Int64, start: Int64, tokenLength: Int64): Float64"
source = "src/lib_json_native_scanner.cj"
needle = "func parseFloat64(input: CPointer<UInt8>"
stability = "stable-optional-backend-seam"
compatibility = "member of a newly added interface; future additions must preserve implementer compatibility"
contract = "Return NaN to decline and let the portable parser handle the token; the built-in bridge performs no Cangjie calls or blocking/I/O work."
consumer = "Native backend implementers"

[[api]]
package = "yjson"
kind = "cangjie-function"
symbol = "installJsonNativeFloatParserBackend"
signature = "public func installJsonNativeFloatParserBackend(backend: JsonNativeFloatParserBackend): Unit"
source = "src/lib_json_native_scanner.cj"
needle = "public func installJsonNativeFloatParserBackend"
stability = "stable-optional-backend-seam"
compatibility = "additive public function"
contract = "Installs one process-global Float64 backend; install before concurrent decoding and do not mix backend ownership models."
consumer = "Optional native package and advanced backend integrations"

[[api]]
package = "yjson"
kind = "cangjie-function"
symbol = "uninstallJsonNativeFloatParserBackend"
signature = "public func uninstallJsonNativeFloatParserBackend(): Unit"
source = "src/lib_json_native_scanner.cj"
needle = "public func uninstallJsonNativeFloatParserBackend"
stability = "stable-optional-backend-seam"
compatibility = "additive public function"
contract = "Removes the process-global Float64 backend; callers must ensure no decoder is using it."
consumer = "Optional native package and advanced backend integrations"

[[api]]
package = "yjson_native"
kind = "cangjie-function"
symbol = "enableYJsonNative"
signature = "public func enableYJsonNative(): Unit"
source = "packages/yjson_native/src/native_backend.cj"
needle = "public func enableYJsonNative(): Unit"
stability = "stable-opt-in"
compatibility = "existing additive activation now installs the Float64 backend together with the full native scanner set"
contract = "Activates the process-global full native backend bundle once; activation modes are mutually exclusive and callers must finish setup before concurrent decoding."
consumer = "Applications opting into the complete yjson_native backend"

[[api]]
package = "yjson_native"
kind = "cangjie-function"
symbol = "enableYJsonNativeFloatOnly"
signature = "public func enableYJsonNativeFloatOnly(): Unit"
source = "packages/yjson_native/src/native_backend.cj"
needle = "public func enableYJsonNativeFloatOnly"
stability = "stable-opt-in"
compatibility = "additive public function"
contract = "Installs only the bounded native Float64 parser; it is mutually exclusive with the full and numeric-only activation modes."
consumer = "Isolated Float64 measurements or deployments"

[[api]]
package = "yjson_native"
kind = "cangjie-function"
symbol = "disableYJsonNativeFloatOnly"
signature = "public func disableYJsonNativeFloatOnly(): Unit"
source = "packages/yjson_native/src/native_backend.cj"
needle = "public func disableYJsonNativeFloatOnly"
stability = "stable-opt-in"
compatibility = "additive public function"
contract = "Idempotently removes the FloatOnly activation; it rejects attempts to disable a different active mode."
consumer = "Isolated Float64 measurements or deployments"

[[api]]
package = "yjson_native / C ABI"
kind = "c-function"
symbol = "YJ_JSON_ParseDouble"
signature = "double YJ_JSON_ParseDouble(const uint8_t*, int64_t, int64_t, int64_t)"
source = ["native/yjson_scanner.h", "native/yjson_scanner.c"]
needle = "YJ_JSON_ParseDouble"
stability = "stable-native-bridge"
compatibility = "additive C symbol and header declaration"
contract = "Parses one already validated JSON number token of at most 256 bytes; invalid bounds, malformed token text, or declined input return NaN."
consumer = "yjson_native @FastNative bridge"

[[api]]
package = "yjson"
kind = "cangjie-constructor"
symbol = "JsonReadConfig.init"
signature = "public init(unknownFieldPolicy!: JsonUnknownFieldPolicy = Ignore, duplicateKeyPolicy!: JsonDuplicateKeyPolicy = LastWins, numberPolicy!: JsonNumberPolicy = Int64WhenExact, includeErrorLocation!: Bool = true, maxDepth!: Int64 = 256, maxPolymorphicObjectBytes!: Int64 = 0, maxStringBytes!: Int64 = 0, maxBytes!: Int64 = 0)"
source = "src/lib_json_value.cj"
needle = "maxStringBytes!: Int64 = 0,"
stability = "stable-major-version-config-constructor"
compatibility = "breaking versus pre-1.0 snapshots: constructor parameters were added and maxPolymorphicObjectBytes default changed from 16 MiB to unlimited; rebuild consumers against the 1.0.0-rc.1 source candidate"
contract = "All resource limits reject negative values. Zero means unlimited; positive values opt into the corresponding parse-time budget."
consumer = "All applications constructing JsonReadConfig"

[[api]]
package = "yjson"
kind = "cangjie-config-field"
symbol = "JsonReadConfig.maxPolymorphicObjectBytes"
signature = "public let maxPolymorphicObjectBytes: Int64"
source = "src/lib_json_value.cj"
needle = "public let maxPolymorphicObjectBytes: Int64"
stability = "stable-config-field"
compatibility = "existing snapshot field; 1.0.0-rc.1 changes the default from 16 MiB to 0 and extends enforcement to public root-container parse paths"
contract = "Maximum raw byte span of the root array or object subtree, including nested content and excluding surrounding whitespace; 0 means unlimited."
consumer = "Applications bounding polymorphic or container-shaped JSON values"

[[api]]
package = "yjson"
kind = "cangjie-config-field"
symbol = "JsonReadConfig.maxStringBytes"
signature = "public let maxStringBytes: Int64"
source = "src/lib_json_value.cj"
needle = "public let maxStringBytes: Int64"
stability = "stable-config-field"
compatibility = "field and constructor parameter added for 1.0.0-rc.1; snapshot consumers must rebuild"
contract = "Maximum decoded UTF-8 byte length of each JSON string value or object key; 0 means unlimited; overflow uses string_too_large."
consumer = "Applications parsing untrusted JSON"

[[api]]
package = "yjson"
kind = "cangjie-config-field"
symbol = "JsonReadConfig.maxBytes"
signature = "public let maxBytes: Int64"
source = "src/lib_json_value.cj"
needle = "public let maxBytes: Int64"
stability = "stable-config-field"
compatibility = "field and constructor parameter added for 1.0.0-rc.1; snapshot consumers must rebuild"
contract = "Maximum input document bytes for memory and stream readers; 0 means unlimited; overflow uses document_too_large."
consumer = "Applications parsing untrusted JSON"

[[api]]
package = "yjson_native / C ABI"
kind = "c-function"
symbol = "YJ_JSON_ValidateLimits"
signature = "int32_t YJ_JSON_ValidateLimits(const uint8_t*, int64_t, int64_t, int64_t, int64_t, int64_t, uint32_t*, int64_t*)"
source = ["native/yjson_compact.h", "native/yjson_compact.c"]
needle = "YJ_JSON_ValidateLimits"
stability = "stable-native-bridge"
compatibility = "additive C symbol; existing parse symbols remain unchanged"
contract = "Allocation-free resource-limit validation shared by native backends; every zero limit means unlimited."
consumer = "Custom Native and yyjson limited parse entry points"

[[api]]
package = "yjson_native / C ABI"
kind = "c-function"
symbol = "YJ_Compact_ParseWithLimits"
signature = "int32_t YJ_Compact_ParseWithLimits(const uint8_t*, int64_t, uint32_t, int64_t, int64_t, int64_t, int64_t, uint64_t*, uint32_t*, uint32_t*, int64_t*)"
source = ["native/yjson_compact.h", "native/yjson_compact.c"]
needle = "YJ_Compact_ParseWithLimits"
stability = "stable-native-bridge"
compatibility = "additive C symbol; YJ_Compact_Parse is preserved"
contract = "Applies document, decoded-string, and root-container byte limits before building the Custom Native DOM."
consumer = "yjson_native resource-limited parsing"

[[api]]
package = "yjson_yyjson / C ABI"
kind = "c-function"
symbol = "YJ_Yyjson_ParseWithLimits"
signature = "int32_t YJ_Yyjson_ParseWithLimits(const uint8_t*, int64_t, uint32_t, int64_t, uint32_t, int64_t, int64_t, int64_t, uint64_t*, uint32_t*, int64_t*)"
source = ["native/yjson_yyjson.h", "native/yjson_yyjson.c"]
needle = "YJ_Yyjson_ParseWithLimits"
stability = "stable-native-bridge"
compatibility = "additive C symbol; YJ_Yyjson_Parse is preserved"
contract = "Applies the shared limits before yyjson allocates a DOM; error codes match the portable and Custom Native facades."
consumer = "yjson_yyjson resource-limited parsing"
